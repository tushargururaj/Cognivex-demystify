
import type {NextConfig} from 'next';

const nextConfig: NextConfig = {
  /* config options here */
  typescript: {
    ignoreBuildErrors: true,
  },
  eslint: {
    ignoreDuringBuilds: true,
  },
  images: {
    remotePatterns: [
      {
        protocol: 'https',
        hostname: 'placehold.co',
        port: '',
        pathname: '/**',
      },
      {
        protocol: 'https',
        hostname: 'images.unsplash.com',
        port: '',
        pathname: '/**',
      },
      {
        protocol: 'https',
        hostname: 'picsum.photos',
        port: '',
        pathname: '/**',
      },
    ],
  },
  env: {
    NEXT_PUBLIC_GCLOUD_PROJECT: process.env.GCLOUD_PROJECT,
    NEXT_PUBLIC_GCLOUD_LOCATION: process.env.GCLOUD_LOCATION,
    NEXT_PUBLIC_GCLOUD_SERVICE_ACCOUNT_CREDS: process.env.GCLOUD_SERVICE_ACCOUNT_CREDS,
  }
};

export default nextConfig;
